# Blob Drop — Design

The full design document for this game lives in [`DESIGN.md`](DESIGN.md)
(uppercase) in this same folder, to match the project brief. It covers the game
concept, mechanics, controls, the testable API, and the assumptions made.

This lowercase `design.md` exists only to satisfy the repo's per-game naming
convention and points at that document so nothing is duplicated.
